# ==============================================================================
# CuraMed Platform - Environment Variables Configuration
# ==============================================================================

# Backend Port and Host
HOST=0.0.0.0
PORT=8000

# MongoDB Atlas Connection URI
# Example: mongodb+srv://<username>:<password>@cluster0.mongodb.net/?retryWrites=true&w=majority
# (Leave empty to use high-reliability local in-memory fallback store during development)
MONGODB_URI=mongodb+srv://mahekchaurasia03_db_user:<NA9bQujd00Zk0yqL>@cluster0.cm49swi.mongodb.net/?appName=Cluster0
MONGODB_DB_NAME=curamed_db

# JWT Security
SECRET_KEY=curamed-super-secret-production-quality-jwt-key-2026
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=10080

# Google Gemini API Key (For Post-Inference Educational Summaries)
# Optional: If omitted, the platform uses deterministic rule-based educational synthesis
GEMINI_API_KEY=

# Frontend API URL (Used by Vite during client build)
VITE_API_URL=http://localhost:8000/api
